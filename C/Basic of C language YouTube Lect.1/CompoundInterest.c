// Program to find compound interest
#include <stdio.h>
int fun(int CI){
    CI=prin* pow((1+r/n),r*n);
    printf("%d",&CI);
    return 0; 
}
int main()
{
    int prin,t,r,n,z,CI;
    printf("enter principal::");
    scanf("%d",&prin);
    printf("enter rate::");
    scanf("%d",&r);
    printf("enter time::");
    scanf("%d",&t);
    printf("enter number of times interest applied per time period::");
    scanf("%d",&n);
    z = fun(int CI);
    return 0;
}